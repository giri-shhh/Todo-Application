spring:
  application:
    name: todo-application
  cloud:
    config:
      discovery:
        enabled: true
        service-id: todo-central-config
        fast-fail: true
    retry:
      max-attempts: 10000
      max-interval: 1000